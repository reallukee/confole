name: Test

on:
  push:
    branches:
      - "**"
    paths:
      - "**/*.fsi"
      - "**/*.fs"
      - "**/*.fsproj"
      - "**/*.cs"
      - "**/*.csproj"
      - "**/*.sln"
      - "**/*.slnx"

      - "!**/dev/**"
      - "!**/examples/**"
      - "!**/templates/**"
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "10.0.x"

      - name: Restore projects
        run: |
          dotnet restore test/confole.test --ignore-failed-sources
          dotnet restore test/confole.sharp.test --ignore-failed-sources

      - name: Build projects
        run: |
          dotnet build test/confole.test --no-restore --configuration Release
          dotnet build test/confole.sharp.test --no-restore --configuration Release

      - name: Test projects
        run: |
          dotnet test test/confole.test --no-build --configuration Release
          dotnet test test/confole.sharp.test --no-build --configuration Release
